<?xml version="1.0" encoding="utf-8"?>

<euphoria.psycho.browser.DisplayToolbar xmlns:android="http://schemas.android.com/apk/res/android"
                                        xmlns:app="http://schemas.android.com/apk/res-auto"
                                        xmlns:tools="http://schemas.android.com/tools"
                                        android:id="@+id/appbar"
                                        android:layout_width="match_parent"
                                        android:layout_height="56dp"
                                        android:clipChildren="false">

    <FrameLayout
            android:id="@+id/urlbar"
            android:layout_width="match_parent"
            android:layout_height="56dp"
            android:background="@drawable/animated_background"
            android:elevation="4dp"
            app:layout_scrollFlags="scroll|enterAlways|snap|exitUntilCollapsed"
            android:clipChildren="false">

        <include
                android:id="@+id/toolbarContent"
                layout="@layout/toolbar"/>

        <euphoria.psycho.browser.AnimatedProgressBar
                android:id="@+id/progress"
                style="@style/Base.Widget.AppCompat.ProgressBar.Horizontal"
                android:layout_width="match_parent"
                android:layout_height="3dp"
                android:progressDrawable="@drawable/photon_progressbar"
                app:shiftDuration="@integer/progress_shift_duration"
                app:wrapShiftDrawable="true"
                android:layout_gravity="bottom"
                android:layout_marginBottom="-1dp"
                android:importantForAccessibility="yes"
                tools:progress="50"/>
    </FrameLayout>

</euphoria.psycho.browser.DisplayToolbar>